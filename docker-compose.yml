services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: stockflow-front
    ports:
      - "5173:5173"
    environment:
      VITE_BASE_URL: ${VITE_BASE_URL}
    volumes:
      - .:/app
      - /app/node_modules
    restart: unless-stopped
